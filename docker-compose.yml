version: '3'

services:
  app:
    build: .
    container_name: brubank-invoice
    ports:
      - '${PORT}:${PORT}'
    volumes:
      - .:/app
      - /app/node_modules
    depends_on:
      - database
      - cache

  database:
    image: postgres
    container_name: brubank-invoice-database
    environment:
      POSTGRES_USER: ${DATABASE_USERNAME}
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD}
      POSTGRES_DB: ${DATABASE_NAME}
    ports:
      - "${DATABASE_PORT}:${DATABASE_PORT}"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    command:
        [
            "-c",
            "log_statement=none",
            "-c",
            "log_destination=stderr"
        ]

  cache:
    image: redis:alpine
    container_name: brubank-invoice-redis
    ports:
      - "${CACHE_PORT}:${CACHE_PORT}"
    environment:
      REDIS_HOST: ${CACHE_HOST}"
      REDIS_PORT: ${CACHE_PORT}"
    volumes:
      - cache_data:/data

volumes:
  postgres_data:
  cache_data:
